export class Scheduler {
  constructor(store) {
    this.store = store;
    this.isRunning = false;
  }
  
  start() {
    // TODO: Implement me
    console.log('Scheduler started (but not really)');
  }
}